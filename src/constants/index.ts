import { Fonts } from "../interfaces";

export const REGISTERED_FONTS: Fonts = {
  'font-sans': 'Sans Serif',
  'font-serif': 'Serif',
  'font-mono': 'Mono',
};

export const DEFAULT_FONT = 'font-serif';
